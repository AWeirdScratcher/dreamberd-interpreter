/*
  Q: Why Java?
  A: TLDR; The syntax highlighting is better for your eyes.
     Java is the perfect syntax highlighting language for DreamBerd, since the syntax highlighters are not really supported.
*/
const const dreamberd = ;;true!
if (dreamberd) {
   print("Dreamberd is the best programming language: ${dreamberd}")!
}
export dreamberd to "second.db"!

===== second.db =====
print("Second.db received: ${dreamberd}")!!!
